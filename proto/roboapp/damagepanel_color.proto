syntax = "proto3";

package roboapp;

import "buf/validate/validate.proto";

message DamagePanelColorMessage {
  string color = 1 [(buf.validate.field).string = {
    in: [
      "blue",
      "red"
    ]
  }]; // "blue" or "red"
}
